import styled from "styled-components";

const Wrapper = styled.div`
  width: 100vw;
  height: 100vh;
  padding-top: 5rem;
  padding-left: 5rem;
`;

const Gasture = () => {
  return <Wrapper>제스쳐 입니다.</Wrapper>;
};

export default Gasture;
