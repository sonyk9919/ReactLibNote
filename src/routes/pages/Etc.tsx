import styled from "styled-components";

const Wrapper = styled.div`
  width: 100vw;
  height: 100vh;
  padding-top: 5rem;
  padding-left: 5rem;
`;

const Etc = () => {
  return <Wrapper>기타입니다</Wrapper>;
};

export default Etc;
